{
  "name": "mobx-validated-field",
  "version": "5.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "test": "mocha --no-deprecation --reporter spec --compilers ts:ts-node/register \"./test/**/*.test.ts\" --recursive --watch",
    "testone": "mocha --no-deprecation --reporter spec --compilers ts:ts-node/register --watch",
    "build": "tsc -p tsconfig.build.json"
  },
  "peerDependencies": {
    "mobx": "^5.9.0"
  },
  "devDependencies": {
    "@types/chai": "^4.1.2",
    "@types/mocha": "^2.2.48",
    "chai": "^4.1.2",
    "mocha": "^5.0.0",
    "ts-node": "^4.1.0",
    "typescript": "^2.7.1"
  },
  "dependencies": {}
}
